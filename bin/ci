#!/bin/sh -eux

# Master CI build script

# If the branch build does something you dont want in the master build,
# You can call bin/ci.common instead
bin/ci.branches

# Do some additional master stuff if necessary, like publishing artifacts
